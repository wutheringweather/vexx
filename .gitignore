node_modules/
out/
release/
build/runtime-secret-seed.json
dist/
*.log
.DS_Store
Thumbs.db
*.tsbuildinfo

# electron-vite writes a timestamped config next to the real one while it runs
electron.vite.config.*.mjs

# Never commit anything that could carry key material or a provider token.
.env
.env.local
keystore.json
state.json
audit.jsonl
lessons.json
conversations.json

# Editor and local tooling
.vscode/
.idea/
.claude/
